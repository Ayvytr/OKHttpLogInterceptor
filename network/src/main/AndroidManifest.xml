<manifest package="com.ayvytr.network.interceptor"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application>
        <provider
            android:name="com.ayvytr.network.provider.ContextProvider"
            android:authorities="${applicationId}.network.cp" />
    </application>
</manifest>
